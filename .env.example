# Server
PORT=4000
CLIENT_ORIGIN=*

# Auth
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d

# Uploads
UPLOAD_DIR=uploads
MAX_UPLOAD_MB=15
# Voice message max length in seconds (enforced client-side; informational for API)
VOICE_MAX_SECONDS=60
